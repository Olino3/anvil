---
name: sync
description: Fix drift between sprint ticket files and README status tables
allowed-args: phase name, version, or prefix
---

Invoke the `anvil:sync` skill with the provided phase argument to sync the sprint README.
